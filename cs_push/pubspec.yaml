name: cs_push
description: CS 框架推送通知模块 - Firebase FCM token 注册、前台/后台消息接收。
version: 1.0.0
homepage: https://github.com/bryanpeng-777/cs

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  cs_core:
    path: ../cs_core
  cs_auth:
    path: ../cs_auth
  firebase_core: ^3.0.0
  firebase_messaging: ^15.0.0
  supabase_flutter: ^2.0.0
  package_info_plus: ^8.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
